{
  "frontend/src/App.tsx": "Main application component handling auth flow, public customer portal route detection, role-based sidebar navigation (admin/staff), profile setup modal, and access-denied screen for unauthorized roles.",
  "frontend/src/components/AdminDashboard.tsx": "Admin dashboard with optional `userRole` prop, period-filtered metric cards (sales, payments, balance, customers), pending balance summary, Export CSV (using `exportTransactionsToCSV`) and Export PDF (using `downloadReportPDF`) buttons, and embedded customer list.",
  "frontend/src/components/PendingCustomersList.tsx": "Pending balances page listing customers with outstanding balances; reminder buttons relabeled to \"Send In-App Reminder\" (singular) and \"Send Bulk In-App Reminders\" (plural), with toast messages updated to reference in-app delivery.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time login modal that collects the user's name and saves their profile with the default \"customer\" role, omitting the optional customerId field to match the backend type.",
  "frontend/src/components/CustomerList.tsx": "Searchable customer list with balance badges, inline Add Customer dialog (no external AddCustomerDialog dependency), copy-portal-link button per row, and admin-only delete.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — user profiles, customers, transactions (including useGetCustomerTransactions and useGetReceipt restored), notifications, pending reminders (useSendPendingReminder, useSendBulkPendingReminders restored), dashboard stats (with StatsPeriod export and useDashboardStats alias), kg rate management, and public customer portal view.",
  "frontend/src/utils/csvExport.ts": "Utility module that exports transaction data to CSV format, generating a downloadable file with columns for customer name, ID, phone, date, weight, rate, total price, payment, payment type, and remaining balance.",
  "frontend/src/components/AdminSettings.tsx": "Admin-only settings page for configuring the rate per kg, fetching the current rate and saving updates via the backend, with success/error feedback.",
  "frontend/src/components/CustomerPortalView.tsx": "Public customer portal (no login required) showing balance, transaction history, and in-app notifications by customer ID; notification section relabeled to \"In-App Notifications\" and a footer note added clarifying no login is needed and customers are identified by phone number.",
  "frontend/src/components/TransactionForm.tsx": "New transaction form that auto-populates the rate field from the backend's current kg rate (read-only), removes the rate from the mutation payload since the backend uses its own stored rate.",
  "frontend/src/components/EditTransactionModal.tsx": "Edit transaction modal that removes the rate field from the mutation payload (backend uses currentKgRate internally) and shows the current shop rate as a read-only display.",
  "frontend/src/components/ReceiptView.tsx": "Receipt display component showing full transaction details with barcode visual, customer info, order details, payment summary, and CLEAR/PENDING status badge, with Print and Download buttons that both call the pdfExport utility; works for both Admin and Staff roles.",
  "frontend/src/components/PeriodSelector.tsx": "Period selector toggle component (daily/weekly/monthly) importing StatsPeriod type from useQueries using a type-only import.",
  "frontend/src/components/LoginPage.tsx": "Login page for Admin and Staff with Internet Identity sign-in, updated role cards to clearly label Admin/Staff as requiring secure login and Customer as needing no login, plus an informational note explaining customers are identified by phone number with no sign-in required.",
  "frontend/src/components/NotificationBell.tsx": "Bell icon button with unread count badge that opens a popover of in-app notifications; aria-label and title updated to reference \"in-app notifications\" instead of generic messaging.",
  "frontend/src/components/NotificationList.tsx": "Popover list of in-app notifications with mark-all-read action; header renamed to \"In-App Notifications\", empty state updated to clarify notifications come from the shop in-app, and toast message updated accordingly.",
  "frontend/src/utils/pdfExport.ts": "PDF receipt export utility that generates a print-ready HTML receipt with barcode-style invoice number, customer details, weight/rate/pricing, payment info, balance status (CLEAR/PENDING), timestamp, and shop name, opened in a new window for printing/saving.",
  "frontend/src/utils/reportPdfExport.ts": "New PDF report export utility for the admin dashboard that generates a formatted HTML report with period-filtered transaction summary metrics, transaction details table, and customer balance list (with CLEAR/PENDING status), opened in a new window for printing/saving.",
  "frontend/src/components/TransactionList.tsx": "Searchable, sortable transaction table showing all transactions with customer info, weight, rate, totals, payment type, balance, and status; Download Receipt button visible to both Admin and Staff; Edit/Delete buttons Admin-only; `EditTransactionModal` receives the required `open` prop."
}